# DocLens backend — pinned dependencies (exact versions, no ranges).
# Python 3.12. Regenerate with: pip install -r requirements.txt
# cache-bust: 2026-07-19

# --- Web framework / server ---
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.50.2

# --- Data validation / settings ---
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Rate limiting ---
slowapi==0.1.10
limits==5.8.0
redis==8.0.1

# --- Observability ---
structlog==26.1.0
sentry-sdk==2.64.0

# --- LLM (Groq) — language generation only ---
groq==0.36.0

# --- Config / HTTP client ---
python-dotenv==1.2.2
httpx==0.28.1

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0

# --- Supabase & Document Agent dependencies ---
supabase==2.31.0
pyjwt[crypto]==2.13.0
python-multipart==0.0.31

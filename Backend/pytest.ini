[pytest]
# Tests import the package as `Backend.<module>`, so run from the repo root
# (rootdir is set to the parent of Backend via the invocation cwd).
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning
